# The wordmark SVG is disabled (empty string, not deleted, so the E25DX theme
# default doesn't merge back in) — the header logo already carries the name.
svg: ""

badge: Written by Doc Holiday

# The <span> gets the gradient text treatment (see hero.css).
title: CrewAI, explained at the <span>execution level</span>.

description: A field guide to what actually happens when a CrewAI crew or flow runs — task ordering, the agent loop, context flow, concurrency, flow scheduling, and where state lives. Written by exploring the source directly, page by page.

btn_primary:
  label: Read the guide
  url: docs/the-big-picture/

btn_secondary:
  label: CrewAI on GitHub
  url: https://github.com/crewAIInc/crewAI
